[build]
[profile.dev]
debug = 1

[profile.dev.package."*"]
# Compile dependencies with optimization even in dev mode
# This makes GTK/WebKit bindings much faster to run without full release build
opt-level = 3
debug = false
